{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "pnpm exec tsup src/index.ts --format esm --dts --clean --external @repo/database",
    "start": "node startup.mjs"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/cors": "^11.2.0",
    "@repo/database": "workspace:*",
    "fastify": "^5.7.1"
  },
  "devDependencies": {
    "@types/node": "^24.10.9",
    "tsup": "^8.5.1",
    "tsx": "^4.21.0",
    "typescript": "~5.9.3"
  }
}
