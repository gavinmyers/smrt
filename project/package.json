{
  "name": "smrt-workspace",
  "private": true,
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "local": "docker compose -f docker-compose.local.yml --env-file .env.local --env-file .env.local.secrets up -d --wait --remove-orphans && pnpm run db:push && dotenv -e .env.local -e .env.local.secrets -- pnpm exec turbo dev",
    "dev": "docker compose -f docker-compose.dev.yml --env-file .env.dev --env-file .env.dev.secrets up --build --wait --remove-orphans",
    "prod": "docker compose -f docker-compose.prod.yml --env-file .env.prod --env-file .env.prod.secrets up --build -d --remove-orphans",
    "test": "pnpm run build && docker compose -f docker-compose.test.yml --env-file .env.test --env-file .env.test.secrets up -d --wait --build --renew-anon-volumes --remove-orphans && pnpm run db:reset && pnpm run test:unit && pnpm run test:e2e && docker compose -f docker-compose.test.yml --env-file .env.test --env-file .env.test.secrets down --volumes --remove-orphans",
    "build": "pnpm exec turbo build",
    "db:migrate": "dotenv -e .env.local -e .env.local.secrets -- pnpm exec prisma migrate dev --schema ./packages/database/prisma/schema.prisma",
    "db:reset": "dotenv -e .env.test -e .env.test.secrets -- pnpm exec prisma db push --force-reset --accept-data-loss",
    "db:push": "dotenv -e .env.local -e .env.local.secrets -- pnpm exec prisma db push --schema ./packages/database/prisma/schema.prisma",
    "db:generate": "pnpm exec prisma generate --schema ./packages/database/prisma/schema.prisma",
    "lint": "pnpm exec biome check .",
    "test:unit": "dotenv -e .env.test -e .env.test.secrets -- pnpm exec turbo test",
    "test:e2e": "dotenv -e .env.test -e .env.test.secrets -- pnpm exec playwright test",
    "verify": "pnpm lint && pnpm test:unit && pnpm build && pnpm test:e2e"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "@playwright/test": "^1.57.0",
    "@types/pg": "^8.16.0",
    "dotenv": "^17.2.3",
    "dotenv-cli": "^11.0.0",
    "prisma": "^7.2.0",
    "turbo": "^2.7.5"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.2.0",
    "@prisma/client": "^7.2.0",
    "pg": "^8.17.1"
  }
}
