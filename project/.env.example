# Database
DB_USER=user
DB_PASSWORD=password
DB_NAME=smrt
DB_HOST=localhost
DB_PORT=5432

# Database connection string (constructed from above or 'memory')
DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?schema=public"
# DATABASE_URL="memory"

# API
PORT=3001
HOST=0.0.0.0
CORS_ORIGIN=http://localhost:5173
COOKIE_SECRET=dev-secret-at-least-32-chars-long-and-secure
