# Database
DB_USER=user
DB_PASSWORD=password
DB_NAME=smrt
DB_HOST=localhost
DB_PORT=5432
DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?schema=public"

# API
PORT=3001
HOST=localhost
CORS_ORIGIN=http://localhost:5173
COOKIE_SECRET=dev-secret-at-least-32-chars-long-and-secure
LOG_LEVEL=info

# Web
VITE_PORT=5173
API_URL=http://localhost:3001

# Docker specific (used in compose files)
API_PORT=3002
WEB_PORT=5174
INTERNAL_PORT=3001
